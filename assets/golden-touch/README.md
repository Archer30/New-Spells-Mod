# Golden Touch small icon

`SpellInt-96.png` reuses Golden Touch's existing `spells.def` artwork over the
small-interface background. It replaces the empty `SpellInt.def` frame for
spell ID 95 (DEF frame 96).
